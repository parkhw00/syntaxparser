#ifndef _PARSER_DESC_H
#define _PARSER_DESC_H

#include "parser.h"

const char *description_list (struct element *e, const char *gap, int indent);
const char *description (struct element *e, int indent);

#endif
